# Bloocube AI Service - Minimal env example
# Copy to .env and fill required values.

# Service
AI_SERVICE_PORT=8001
AI_SERVICE_HOST=0.0.0.0

# Backend integration (required)
BACKEND_SERVICE_URL=http://localhost:5000
# If your backend expects an API key header from AI service (optional)
BACKEND_API_KEY=
# If AI service protects its endpoints for backend calls (optional)
AI_SERVICE_API_KEY=

# AI Providers
# Choose your primary model provider and provide its key
# Options: openai | gemini
PRIMARY_AI_PROVIDER=openai
ENABLE_AI_FALLBACK=true
FALLBACK_AI_PROVIDER=gemini

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo-preview

# Google Gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-pro

# Logging (optional)
LOG_LEVEL=INFO
